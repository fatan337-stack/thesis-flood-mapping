# Geospatial Python project: flood mapping with Sentinel-1 SAR data
# Notebooks 01 to 04 run in Google Colab, where most of these are
# pre-installed. 05_tile_explorer runs locally. The versions below are
# conservative floors; newer compatible versions should also work.

# --- Core geospatial and data stack (used across notebooks) ---
folium>=0.14
geopandas>=0.13
ipywidgets>=8.0
matplotlib>=3.7
numpy>=1.24
pandas>=2.0
pyproj>=3.5
rasterio>=1.3
shapely>=2.0

# --- 02_unet_inference ---
# The U-Net model code is cloned from the STURM-Flood repository at
# runtime and brings its own requirements; these are the direct extras.
# tensorflow-io is installed inside the notebook itself.
tensorflow>=2.13
tqdm>=4.65
requests>=2.31

# --- 03_random_forest ---
scikit-learn>=1.3
joblib>=1.3

# --- 04_otsu_threshold ---
scikit-image>=0.21

# --- 05_tile_explorer (overview maps and basemaps) ---
contextily>=1.3
matplotlib-scalebar>=0.8

# Notes:
# - `google.colab` is provided by the Colab environment and cannot
#   be installed via pip. If running locally, replace the
#   `drive.mount` cell with a local path configuration.
# - IPython is bundled with Jupyter/Colab.
